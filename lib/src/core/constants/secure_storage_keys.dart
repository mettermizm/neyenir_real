/// It's a class that contains static strings that represent the keys for the values that we want to
/// store in the secure storage
class SecureStorageKeys {
  static String authToken = 'auth_token';
}
